--// FullBright Universal Script
--// Feito para administradores e uso universal
--// Funções: Ativar() e Desativar()

local FullBright = {}
FullBright.Enabled = false
FullBright.NormalLightingSettings = {}

--// Função para salvar configurações originais de iluminação
local function SaveLighting()
	local Lighting = game:GetService("Lighting")
	FullBright.NormalLightingSettings.Brightness = Lighting.Brightness
	FullBright.NormalLightingSettings.ClockTime = Lighting.ClockTime
	FullBright.NormalLightingSettings.FogEnd = Lighting.FogEnd
	FullBright.NormalLightingSettings.GlobalShadows = Lighting.GlobalShadows
	FullBright.NormalLightingSettings.Ambient = Lighting.Ambient
end

--// Função para ativar FullBright
function FullBright.Ativar()
	if FullBright.Enabled then return end
	local Lighting = game:GetService("Lighting")
	SaveLighting()

	Lighting.Brightness = 2
	Lighting.ClockTime = 12
	Lighting.FogEnd = 1e10
	Lighting.GlobalShadows = false
	Lighting.Ambient = Color3.new(1, 1, 1)

	FullBright.Enabled = true
	print("[FullBright] Ativado.")
end

--// Função para desativar FullBright
function FullBright.Desativar()
	if not FullBright.Enabled then return end
	local Lighting = game:GetService("Lighting")
	local Normal = FullBright.NormalLightingSettings

	Lighting.Brightness = Normal.Brightness
	Lighting.ClockTime = Normal.ClockTime
	Lighting.FogEnd = Normal.FogEnd
	Lighting.GlobalShadows = Normal.GlobalShadows
	Lighting.Ambient = Normal.Ambient

	FullBright.Enabled = false
	print("[FullBright] Desativado e configurações restauradas.")
end

--// Exemplo de uso manual:
-- FullBright.Ativar()
-- FullBright.Desativar()

return FullBright
